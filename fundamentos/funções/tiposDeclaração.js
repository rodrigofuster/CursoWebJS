// function delaration
function soma(x, y) {
  return x + y;
}

// function espression - anônima
const sub = function (x, y) {
  return x - y;
};

// named function expression
const mult = function mult(x, y) {
  return x * y;
};
